import type { CStatement } from './cStatement';
export interface CIfStatement extends CStatement { readonly kind:'if'; readonly condition:string; readonly thenBranch:readonly CStatement[]; readonly elseBranch:readonly CStatement[]; }
export interface CWhileStatement extends CStatement { readonly kind:'while'; readonly condition:string; readonly body:readonly CStatement[]; }
export function createCIfStatement(id:string,condition:string,thenBranch:readonly CStatement[],elseBranch:readonly CStatement[]=[]):CIfStatement|undefined { if(!id.trim()||!condition.trim())return undefined;return {id:id.trim(),kind:'if',expression:condition.trim(),condition:condition.trim(),thenBranch:[...thenBranch],elseBranch:[...elseBranch],authoritative:true}; }
export function createCWhileStatement(id:string,condition:string,body:readonly CStatement[]):CWhileStatement|undefined { if(!id.trim()||!condition.trim())return undefined;return {id:id.trim(),kind:'while',expression:condition.trim(),condition:condition.trim(),body:[...body],authoritative:true}; }

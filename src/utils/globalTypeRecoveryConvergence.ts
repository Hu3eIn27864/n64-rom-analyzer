import type { TypeRecoveryCandidate } from './typeRecoveryCandidates';
import { evaluateTypeRecoveryConvergence } from './typeRecoveryConvergence';
import { summarizeTypeRecoveryConvergence } from './typeRecoveryConvergenceSummary';
export interface GlobalTypeRecoveryConvergence<T>{readonly converged:boolean;readonly iterations:number;readonly authoritative:boolean;readonly candidates:readonly TypeRecoveryCandidate<T>[];readonly summary:ReturnType<typeof summarizeTypeRecoveryConvergence<T>>;}
export function evaluateGlobalTypeRecoveryConvergence<T>(history:readonly (readonly TypeRecoveryCandidate<T>[])[]):GlobalTypeRecoveryConvergence<T>{const result=evaluateTypeRecoveryConvergence(history);const previous=history.length>1?history[history.length-2]:[];const summary=summarizeTypeRecoveryConvergence(previous,result.candidates);return {converged:result.converged,iterations:result.iterations,authoritative:result.candidates.every(candidate=>candidate.authoritative),candidates:result.candidates,summary};}
